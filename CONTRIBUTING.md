# FlowCraft에 기여하기

버그 제보, 사용성 개선, 문서 수정과 코드 기여를 환영합니다.

## 시작하기 전에

1. 기존 Issue에 같은 내용이 있는지 확인합니다.
2. 큰 기능이나 데이터 구조 변경은 구현 전에 Issue로 방향을 먼저 논의합니다.
3. 보안 취약점은 공개 Issue가 아닌
   [Security Advisory](https://github.com/sleeplesshan/flowcraft-task-studio/security/advisories/new)로
   알려주세요.

## 개발 환경

Node.js 22.13 이상이 필요합니다.

```bash
npm install
npm run dev
```

## 변경 작업 순서

1. 저장소를 fork하고 변경 목적이 드러나는 브랜치를 만듭니다.
2. 한 PR에는 하나의 문제나 기능만 담습니다.
3. 동작이 바뀌면 관련 단위 테스트 또는 브라우저 테스트를 추가합니다.
4. 아래 검증을 통과시킵니다.
5. PR 양식에 변경 이유와 확인 방법을 작성합니다.

```bash
npm test
npm run lint
npx tsc --noEmit
npm run test:site
```

화면 흐름, 그래프 편집, 클립보드 또는 다운로드 동작을 바꿨다면
`npm run test:e2e`도 실행해 주세요.

## 프로젝트 원칙

- 일반 화면의 문구는 쉬운 한국어를 우선합니다.
- 노드는 에이전트가 아니라 **작업 계약**을 나타냅니다.
- 작업 흐름은 순환이 없는 그래프여야 합니다.
- 병렬 그룹은 서로 의존하지 않는 작업만 묶습니다.
- Markdown이 실행 기준 원본이며 그래프 이미지는 보조 자료입니다.
- 앱에서 LLM API를 직접 호출하거나 API 키 입력을 요구하지 않습니다.
- 사용자 입력은 브라우저 로컬 저장 경계를 유지합니다.

## 공개 저장소에 포함하지 않는 자료

다음 자료는 커밋하지 마세요.

- `.dual-brain/` 프로젝트 메모리
- `.openai/hosting.json` 같은 로컬 배포 설정
- 사용자 프롬프트, LLM 응답 JSON과 `localStorage` 데이터
- API 키, 인증 토큰, 이메일 등 개인 정보
- 내부 감사 문서, 전체 브라우저 화면, 테스트 결과와 임시 산출물

## 커밋과 PR

- 커밋 메시지는 변경 목적을 짧게 설명합니다.
- PR에는 무엇을, 왜 바꿨는지와 검증 결과를 적습니다.
- 관련 Issue가 있다면 `Closes #번호`로 연결합니다.
- 리뷰 중 요청된 수정은 가능하면 같은 PR에 반영합니다.

기여를 제출하면 해당 기여가 이 프로젝트의 MIT 라이선스로 배포되는 데
동의한 것으로 간주합니다.
